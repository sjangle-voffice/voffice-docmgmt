package com.voffice.rearch.utils.exception.config;

/**
    Handler config key enum ,mapped in properties file with all the possible handler configurations.
**/
public enum ErrorHandlerConfigKey {
    INVALID_FILE_DELIMETER
}